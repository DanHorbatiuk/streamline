package dev.horbatiuk.streamlineapi.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/v1/devices")
public class DeviceController {

    /*
     * POST /devices — додати новий пристрій.
     * GET /devices — список пристроїв користувача.
     * GET /devices/{id} — інформація про конкретний пристрій.
     * PUT /devices/{id} — оновлення пристрою.
     * DELETE /devices/{id} — видалити пристрій.
     */

}
